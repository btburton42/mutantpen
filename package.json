{
  "name": "bookpress",
  "version": "0.0.1",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "dev": "nuxt",
    "deploy": "nuxt build && sls deploy",
    "test": "export AWS_REGION=us-east-1 && export STAGE=test && jest",
    "test:watch": "npm run test -- --watch --no-cache"
  },
  "author": "Tyler Burton",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.3",
    "nuxt": "^1.4.2",
    "serverless-apigw-binary": "^0.4.4",
    "serverless-http": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "serverless-domain-manager": "^2.6.0"
  }
}
